/** De default state van de applicatie. */
const defaultState = 0

/**
 * Dit is de reducer.
 * De reducer accepteert de huidige state en een action object. 
 * In dit geval is de state een Number, zoals de `defaultState` aangeeft.
 * De function returnt dus ook altijd een Number
 * 
 * Opdracht:
 * Implementeer de reducer zodat voor elke action uit `actions.js` een state 
 * gereturnt wordt (een nummer) dat voldoet aan de beschrijving van de action.
 * Als er onbekende action type wordt mee gegeven, return dan de huisige state.
 */
